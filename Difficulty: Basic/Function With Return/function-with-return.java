class Solution {
    // Function that takes an integer n and returns double its value
    public int returnValueFunction(int n) {
        // Use the return keyword to output twice the value of n
        return n * 2;
    }
}