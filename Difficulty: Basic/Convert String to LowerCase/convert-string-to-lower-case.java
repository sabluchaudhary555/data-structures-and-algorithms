class Solution {
    static String toLower(String s) {
        // Use the built-in method to convert the entire string to lowercase
        // This handles all uppercase characters (A-Z) in the string
        return s.toLowerCase();
    }
}